DATABASE_URL=postgresql://bank:bank@localhost:5432/transaction
KAFKA_BOOTSTRAP_SERVERS=localhost:29092
SERVICE_NAME=transaction-service
LOG_LEVEL=INFO
PORT=8004
# URL do account-service, para a chamada sincrona GET /v1/accounts/{id}
# que valida ACCOUNT_NOT_ACTIVE (specs/business/06-pixkey-transaction-crud.md).
ACCOUNT_SERVICE_URL=http://localhost:8002
# URL do pix-key-service, para a chamada sincrona GET /v1/pix-keys/lookup
# que valida pix_key_destino (specs/business/15-validacao-chave-destino.md).
PIX_KEY_SERVICE_URL=http://localhost:8003
