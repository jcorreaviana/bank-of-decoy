DATABASE_URL=postgresql://bank:bank@localhost:5432/onboarding
KAFKA_BOOTSTRAP_SERVERS=localhost:29092
SERVICE_NAME=onboarding-service
LOG_LEVEL=INFO
PORT=8001
# Chave simetrica Fernet para criptografar CPF em repouso (specs/business/10-criptografia-cpf.md).
# Mesma chave usada no account-service. Gerar com:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CPF_ENCRYPTION_KEY=changeme-generate-a-real-fernet-key
